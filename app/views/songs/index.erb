<ul>
<% @songs.each do |song|%>
  <li>
    <a href="/songs/<%= song.id %>">Title: <%= song.title %></a>
    Artist: <%= song.artist %>
    Genre: <%= song.genre %>
  </li>
<% end %>
</ul>

<form method="get" action="/songs/new">
  <input type="submit" value="Create New Song">
</form>
